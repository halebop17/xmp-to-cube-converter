# Python
__pycache__/
*.pyc
# Reference/validator core -- not shipped; the plugin is self-contained Lua.
/core
/cli.py

# Generated / local-only artifacts
identity_*.tif
# ...except the identity image bundled inside the plugin (shipped, not generated by users)
!xmp-to-cube.lrdevplugin/identity/identity_*.tif
*.cube
_selftest_*.tif
/tests

# Planning docs (kept locally, not published)
*-PLAN.md

# macOS
.DS_Store
CLAUDE.md
